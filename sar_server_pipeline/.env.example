# Copy to .env and set these for the target server.
DATA_ROOT=/srv/sar-data
JOB_DIR=/srv/jobs
JOB_MANIFEST=/job/job.yaml

# Optional overrides.
SNAP_GPT=/usr/local/snap/bin/gpt
EDL_USER=
EDL_PASS=

# Headless QGIS digitisation preparation.
# REPO_ROOT is the repository root containing Data_Creation, Domain_SSL, and Drift.
REPO_ROOT=..
DIGITISING_REMOTE=bolelang@146.64.214.137
REMOTE_DATA_ROOT=/mnt/storage/bolelang_mount/Joshua/sar-data
DESKTOP_ROOT=/home/bsibolla/Desktop/Joshua
DIGITISING_SECRETS_DIR=./secrets
