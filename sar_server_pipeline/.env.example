# Copy to .env and set these for the target server.
DATA_ROOT=/srv/sar-data
JOB_DIR=/srv/jobs
SNAP_HOST_DIR=/opt/esa-snap
JOB_MANIFEST=/job/job.yaml

# Optional overrides.
SNAP_GPT=/opt/snap/bin/gpt
EDL_USER=
EDL_PASS=
